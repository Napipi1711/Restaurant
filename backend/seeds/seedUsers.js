import bcrypt from 'bcryptjs'; import User from '../models/UserModels.js'; // dùng .js nếu là ES Modules async function seedUsers() { const managerEmail = 'manager1@gmail.com'; const managerExists = await User.findOne({ email: managerEmail }); if (!managerExists) { const hashedManagerPassword = await bcrypt.hash('123456', 10); await User.create({ username: 'Manager', email: managerEmail, password: hashedManagerPassword, role: 'manager', active: true }); console.log('✅ Manager account created'); } else { console.log('ℹ️ Manager already exists'); } } export default seedUsers;